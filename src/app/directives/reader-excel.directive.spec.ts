import { ReaderExcelDirective } from './reader-excel.directive';

describe('ReaderExcelDirective', () => {
  it('should create an instance', () => {
    const directive = new ReaderExcelDirective();
    expect(directive).toBeTruthy();
  });
});
